<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="io.choerodon.issue.infra.mapper.StateMachineSchemeConfigMapper">

    <select id="selectDefault" resultType="io.choerodon.issue.domain.StateMachineSchemeConfigDraft">
        SELECT
        smscd.*
        FROM
        state_machine_scheme_config_draft smscd
        WHERE
        smscd.organization_id = #{organizationId}
        AND smscd.scheme_id = #{schemeId}
        AND smscd.is_default = 1
    </select>

</mapper>
