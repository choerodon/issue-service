spring:
  profiles:
    active: test
mybatis:
  mapperLocations: classpath*:/mapper/*.xml
  configuration: # 数据库下划线转驼峰配置
    mapUnderscoreToCamelCase: true
security:
  basic:
    enabled: false
#    authorize-mode: NONE
